name: <Test> CUDA Build Project
on: [push]
jobs:
  build_artifacts_windows:
    name: <Test> CUDA Build (Windows)
    runs-on: windows-2022
    steps:
      # - name: Get latest code
      #   uses: actions/checkout@v3

      - name: Install CUDA Toolkit
        uses: Jimver/cuda-toolkit@v0.2.10
        id: cuda-toolkit
        with:
          cuda: '12.1.0'

      - name: Checking for nvcc
        shell: pwsh
        run: nvcc -V

      - name: nvidia-smi
        shell: pwsh
        run: nvidia-smi

      # - name: CMake x64
      #   shell: pwsh
      #   run: cmake OpenMP -G "Visual Studio 17 2022" -A x64 CMakeLists.txt -B OpenMP/x64

      # - name: Build x64
      #   shell: pwsh
      #   run: cmake --build OpenMP/x64 --target Multi-Segmenter Multi-Segmenter-Test --config Release && mv OpenMP/x64/Release/* OpenMP/x64

      # - name: Upload artifact
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: build
      #     path: |
      #       OpenMP/Win32/Multi-Segmenter.exe
      #       OpenMP/Win32/Multi-Segmenter-Test.exe
      #       OpenMP/x64/Multi-Segmenter.exe
      #       OpenMP/x64/Multi-Segmenter-Test.exe